apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monarka-store
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Monarka-store/monarka-project.git
    targetRevision: HEAD
    helm:
      releaseName: monarka-store
      valuesFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: monarka

  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
